{
  "name": "adidasstore",
  "version": "1.0.0",
  "description": "Practice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url --no-content-hash",
    "predeploy": "rm -rf dist && parcel build index.html --no-source-maps --public-url --no-content-hash ./",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [
    "HTML",
    "CSS",
    "JS",
    "practice"
  ],
  "author": "Albert Shamsutdinov",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.10"
  }
}
